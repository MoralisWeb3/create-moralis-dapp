import { createMoralisDapp } from './create-moralis-dapp';

describe('createMoralisDapp', () => {
  it('should work', () => {
    expect(createMoralisDapp()).toEqual('create-moralis-dapp');
  });
});
